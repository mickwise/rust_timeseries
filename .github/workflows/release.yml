name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read      # for checkout
  id-token: write     # for OIDC if you switch from token

jobs:
  publish:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: [3.9, 3.10, 3.11, 3.12, 3.13]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable

      - name: Cache cargo & pip
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cache/pip
          key: ${{ runner.os }}-cargo-${{ hashFiles('Cargo.lock') }}

      - name: Build wheels & sdist
        shell: bash
        run: |
          python -m pip install --upgrade pip wheel setuptools maturin
          if [[ "${{ runner.os }}" == "macos-latest" ]]; then
            maturin build --release --sdist --universal2
          else
            maturin build --release --sdist
          fi

      - name: Publish to PyPI
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
        run: |
          python -m pip install --upgrade twine
          twine upload --skip-existing target/wheels/*
          twine upload --skip-existing target/sdist/*
